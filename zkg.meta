[package]
description = Generates notice log when it detects potential remote code execution attempts over HTTP
tags = http
script_dir = scripts
version = 0.0.2
depends =
  zeek >=3.0.0
  https://github.com/nturley3/zeek-http-post-bodies >= 3.1.0
